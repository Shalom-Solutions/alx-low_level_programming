#include <stdlid.h>
#include <stdio.h>

/**
 * main - Entry point
 * Program to print out all single numbers between 0 and 9
 * Return: Always 0(Success)
 */

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
	printf("%d\n", num);
	}
	return (0);
}

